<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="newPromoCalc.miller"
    android:versionCode="52"
    android:versionName="5.2" >

    <uses-sdk android:minSdkVersion="10" />

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name="newPromoCalc.miller.Splash" 
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:name="newPromoCalc.miller.ArmyPromotionPointCalculatorActivity" 
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter >
                
				<action android:name="ct.design.promoCalcPro.STARTER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name="newPromoCalc.miller.apftActivity"></activity>
        <activity android:name="newPromoCalc.miller.E5CalculatorActivity"></activity>
        <activity android:name="newPromoCalc.miller.E6CalculatorActivity"></activity>
        <activity android:name="newPromoCalc.miller.aboutActivity"></activity>
        <activity android:name="newPromoCalc.miller.recentPointsActivity"></activity>
        <activity android:name="org.achartengine.GraphicalActivity"></activity>
        <activity android:name="newPromoCalc.miller.lastYearGraph"></activity>
    </application>

</manifest>